% layout 'auth';
%= tag 'div', id => 'container', class => 'admin_list_pages', => begin
  %= tag 'ul' => begin
    % foreach (@{$postlist}) {
      %= tag 'li' => "$_->{topic}"
    % }
  %= end
%= end
