%= t 'div', id => 'footer' => begin
  <%= link_to 'http://perl.org' => begin %><img src="/img/perl.png" height="15" width="80" alt="Perl Powered" /><% end %>
  &rarr;
  <%= link_to "https://github.com/skryf/Skryf" => begin %>Skryf<% end %> <%= app->config->{version} %>

    %= t 'div', id => 'account' => begin
     % if (my $auth = session 'user') {
      <b>You are currently logged in.</b>
     % }
    %= end

%= end
